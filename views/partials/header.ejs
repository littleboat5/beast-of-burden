<!DOCTYPE html>
<html>
<head>
    
    <!-- must include '/' in front of app.css so it knows to find app.css from the root where public dir is served -->
	<link rel="stylesheet" type="text/css" href="/app.css">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css" integrity="sha384-DNOHZ68U8hZfKXOrtjWvjxusGo9WQnrNx2sqG0tfsghAvtVlRW3tvkXWZh58N9jp" crossorigin="anonymous">
	
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">

<!-- IMPORTANT JQUERY MUST BE PLACED BEFORE BOOTSTRAP.JS !!!! -->
	<script src="https://code.jquery.com/jquery-3.3.1.min.js" integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js" integrity="sha384-smHYKdLADwkXOn1EmN1qk/HfnUcbVRZyYmZ4qpPea6sjB/pTJ0euyQp0Mk8ck+5T" crossorigin="anonymous"></script>

<!-- Make site responsive on mobile/tablet -->
<meta name="viewport" content="width=device-width, initial-scale=1">

<title>YelpBeast</title>
</head>
<body class="main-color">
    
<!-------- Nav bar ----------->

<nav class="navbar navbar-expand-lg navbar-dark" >
<!--<nav class="navbar navbar-expand-lg navbar-light bg-light">-->
    <!--<div class="container"  style="background-color: #e3f2fd;">-->
        <a class="navbar-brand" href="/">YelpBeast</a>
        <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="navbar-toggler-icon"></span>
        </button>

        
        <div id="navbar" class="collapse navbar-collapse">
            <div class="navbar-nav">
              <a class="nav-item nav-link <%= typeof pageName !== 'undefined' && pageName === 'beasts' ? 'active' : '' %>" 
                    href="/Beasts">Home</a>
                    <!--href="/Beasts">Home <span class="sr-only">(current)</span></a>-->
             
              <% if(!currUser){ %>
                  <a class="nav-item nav-link <%= typeof pageName !== 'undefined' && pageName === 'login' ? 'active' : '' %>" 
                    href="/login">Login</a>
                  <a class="nav-item nav-link <%= typeof pageName !== 'undefined' && pageName === 'register' ? 'active' : '' %>" 
                    href="/register">Sign Up</a>
              <% } else { %>
                  <a class="nav-item nav-link " href="#">Signed In As <%= currUser.username %></a>
                  <a class="nav-item nav-link " href="/logout">Logout</a>
              <% } %>
            </div>
        </div>
    <!--</div>-->
</nav>

<!-- FLASH MESSAGES -->
<% if (error && error.length > 0) { %>
    <div class="alert alert-danger" role="alert">
        <%=error%>
    </div>
<% } %>
<% if (success && success.length > 0) { %>
    <div class="alert alert-success" role="alert">
        <%=success%>
    </div>
<% } %>
<% if (warning && warning.length > 0) { %>
    <div class="alert alert-warning" role="alert">
        <%=warning%>
    </div>
<% } %>
                
